# Secrets Management

This project does NOT store real secrets.
All credential fields use placeholder or REDACTED values.

## Before deploying
1. Copy template files and replace placeholders with real values
2. Never commit real credentials
3. Run secret_scan.sh to verify no secrets were committed
